
export default function renderProjectTodos(target){
    console.log("hi");
    // remove all todos from the current page

    // look for the project with the class of active, find its id and use it to render that projects todos from its internal todo array


};